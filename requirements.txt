# Runtime dependencies for the analysis pipeline and API.
# Installing the package itself (`pip install -e .`) is preferred; this file exists
# for deployment targets that expect a requirements.txt, such as Render.
#
# opencv-python-headless rather than opencv-python: the server has no display, and
# the headless build drops the GUI dependencies that otherwise fail to install in a
# slim container.
numpy>=1.26,<3
scipy>=1.11
opencv-python-headless>=4.9
scikit-image>=0.22
scikit-learn>=1.4,<2
pandas>=2.1
pillow>=10.2
joblib>=1.3
pyarrow>=15.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
